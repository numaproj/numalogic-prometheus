{
  "title": "PipelineConf",
  "description": "PipelineConf(*, redis_conf: numaprom._config.RedisConf, prometheus_conf: numaprom._config.PrometheusConf, registry_conf: numaprom._config.RegistryConf)",
  "type": "object",
  "properties": {
    "redis_conf": {
      "$ref": "#/definitions/RedisConf"
    },
    "prometheus_conf": {
      "$ref": "#/definitions/PrometheusConf"
    },
    "registry_conf": {
      "$ref": "#/definitions/RegistryConf"
    }
  },
  "required": [
    "redis_conf",
    "prometheus_conf",
    "registry_conf"
  ],
  "definitions": {
    "RedisConf": {
      "title": "RedisConf",
      "description": "RedisConf(*, host: str, port: int, expiry: int = 300, master_name: str = 'mymaster')",
      "type": "object",
      "properties": {
        "host": {
          "title": "Host",
          "type": "string"
        },
        "port": {
          "title": "Port",
          "type": "integer"
        },
        "expiry": {
          "title": "Expiry",
          "default": 300,
          "type": "integer"
        },
        "master_name": {
          "title": "Master Name",
          "default": "mymaster",
          "type": "string"
        }
      },
      "required": [
        "host",
        "port"
      ]
    },
    "PrometheusConf": {
      "title": "PrometheusConf",
      "description": "PrometheusConf(*, server: str, pushgateway: str)",
      "type": "object",
      "properties": {
        "server": {
          "title": "Server",
          "type": "string"
        },
        "pushgateway": {
          "title": "Pushgateway",
          "type": "string"
        }
      },
      "required": [
        "server",
        "pushgateway"
      ]
    },
    "RegistryConf": {
      "title": "RegistryConf",
      "description": "RegistryConf(*, tracking_uri: str)",
      "type": "object",
      "properties": {
        "tracking_uri": {
          "title": "Tracking Uri",
          "type": "string"
        }
      },
      "required": [
        "tracking_uri"
      ]
    }
  }
}